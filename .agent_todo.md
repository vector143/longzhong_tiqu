- [x] 读取 `LESSONS.md`、`git status`、`git diff --stat`
- [x] 审查已跟踪差异与 staged 测试变更
- [x] 盘点未跟踪新文件并确认提交范围
- [x] 运行秘钥扫描与必要验证
- [x] 自审 diff 并统一提交剩余改动

## 审查这三个命令的优化点与潜在问题
- [x] 审查 `python crawl/investing_monitor.py --monitor --interval 30 --proxy http://127.0.0.1:7897`
- [x] 审查 `python -m crawl.multi_commodity_monitor --interval 30`
- [x] 审查 `python -m monitor.runner --keywords "原油,甲醇,PTA,乙二醇,铜,白银,橡胶,天然橡胶" --no-history`
- [x] 汇总可优化项、潜在风险与建议执行顺序

## 入口优化实现
- [x] 为 `crawl/investing_monitor.py` 补 CLI 数值校验与 `--max-pages`
- [x] 为 `crawl/multi_commodity_monitor.py` 补激进轮询配置提示
- [x] 为 `monitor/runner.py` 补 `--no-history` 冷启动提示与关键词重叠提示
- [x] 补测试并跑通 `ruff`、`black`、`pytest`

## 统一 Rich 方案设计
- [x] 盘点 `unified_monitor.py` 与 `monitor/unified_ui.py` 的当前能力边界
- [x] 对比 `monitor/runner.py`、`monitor/state.py`、`monitor/ui.py`、`monitor/scheduler.py` 的成熟能力
- [x] 设计三路命令统一到单一 Rich 界面的目标架构、兼容策略与演进路径
- [x] 产出 `plans/2026-03-15-unify-monitor-rich-ui-v1.md`
- [x] 运行 `create-plan` 校验脚本并修正格式问题

## 统一 Rich 第一阶段实现
- [x] 为统一 manager / unified UI 先补 failing tests
- [x] 扩展 `monitor.manager` 为可选中源、单源控制的控制平面
- [x] 重写 `monitor.unified_ui` 为“汇总 + 源列表 + 详情”布局
- [x] 补齐 adapter 基类/现有 adapter 的最小控制接口与元数据
- [x] 跑 targeted pytest
- [x] 跑 `ruff check . --fix`、`black .`、`ruff check .`
- [x] 提交本轮实现

## 统一 Rich 第二阶段实现
- [x] 为 `runner runtime` 抽取与 LongZhong 复用先补 failing tests
- [x] 从 `monitor.runner` 抽取可复用的 runtime 构建/启动/停止层
- [x] 让 `LongZhongAdapter` 复用正式 runtime，而不是简化 while 循环
- [x] 把 formal state snapshot 映射到统一控制台所需字段
- [x] 跑 targeted pytest
- [x] 跑 `ruff check . --fix`、`black .`、`ruff check .`
- [x] 提交本轮实现

## 统一 Rich 第三阶段实现
- [x] 为 WSJ / Investing 统一状态粒度与统一入口参数先补 failing tests
- [x] 丰富 `WallStreetCNAdapter` / `InvestingAdapter` 的 detail 状态与控制元数据
- [x] 扩展 `unified_monitor.py` 参数，补齐 Investing 关键运行参数透传
- [x] 让统一 UI 能稳定展示 WSJ / Investing 的关键 detail 字段
- [x] 跑 targeted pytest
- [x] 跑 `ruff check . --fix`、`black .`、`ruff check .`
- [x] 提交本轮实现

## 统一 Rich 第四阶段实现
- [x] 为 WSJ / Investing 的正式控制语义先补 failing tests
- [x] 给 WSJ 补源级 run_now / 聚合状态字段
- [x] 给 Investing 补 next_run / success / backoff 等正式状态字段
- [x] 让统一 UI 展示标准控制状态而不被布局裁掉
- [x] 跑 targeted pytest
- [x] 跑 `ruff check . --fix`、`black .`、`ruff check .`
- [x] 提交本轮实现

## 统一 Rich 第五阶段实现
- [x] 为 runtime/controller 生命周期抽象先补 failing tests
- [x] 在 `monitor.adapter` 补统一 runtime metadata 与 start/stop hooks
- [x] 让 WSJ / Investing 接入 runtime lifecycle、支持 restart reset
- [x] 让统一 UI 展示运行控制摘要与 worker 视图
- [x] 跑 targeted pytest
- [x] 跑 `ruff check . --fix`、`black .`、`ruff check .`
- [x] 提交本轮实现

## 本轮效率优化（统一监控）
- [x] 为 Investing 参数链路补 failing tests（`workers` 透传到底层）
- [x] 为 Investing 频道并发上限补 failing tests（受 `self.max_workers` 限制）
- [x] 修改 `unified_monitor.py`/`monitor/adapters.py`/`crawl/investing_monitor.py`
- [x] 运行 targeted pytest
- [x] 运行 `ruff check . --fix`、`black .`、`ruff check .`、`pytest -q`
- [x] 更新 `LESSONS.md`
- [x] 自审后提交

## 本轮效率优化（二）：Investing 自适应轮询
- [x] 为 unified 入口补自适应参数透传测试
- [x] 为 InvestingAdapter 补空轮询降频测试
- [x] 修改 `unified_monitor.py` 与 `monitor/adapters.py` 实现自适应轮询
- [x] 运行 targeted pytest
- [x] 运行 `ruff check . --fix`、`black .`、`ruff check .`、`pytest -q`
- [x] 更新 `LESSONS.md`
- [x] 自审后提交
